@model NihongoSekaiWebApplication_D11_RT01.Data.ViewModels.ForgotPasswordVM

<div class="container d-flex align-items-center justify-content-center vh-100">
    <div class="col-md-6 col-lg-5 shadow-lg p-5 rounded-4 bg-white">
        <div class="mb-4 text-center">
            <h3 class="fw-bold">Reset Password</h3>
            <p class="text-muted">Let's create new password for you account!'</p>
        </div>

        @if (TempData["Error"] != null)
        {
            <div class="alert alert-danger alert-dismissible fade show" role="alert">
                <strong>Sorry!</strong> @TempData["Error"]
                <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
            </div>
        }

        <form asp-action="ForgotPassword" method="post">
            <div asp-validation-summary="ModelOnly" class="text-danger mb-2"></div>
            <div class="mb-3">
                <label asp-for="Email" class="form-label"></label>
                <input asp-for="Email" class="form-control input-gradient" />
                <span asp-validation-for="Email" class="text-danger"></span>
            </div>

            <button type="submit" class="btn w-100 gradient-outline-btn">
                <i class="bi bi-box-arrow-in-right me-1"></i> Send Reset Link
            </button>

            <hr />

            <p class="text-center">Forgot you account? <a asp-action="Register">Create another one :3</a></p>

            <div class="mt-4 text-center">
                <a asp-controller="Account" asp-action="Login" class="text-muted small">
                    <i class="bi bi-arrow-left-circle"></i> Back to Login Page
                </a>
            </div>
        </form>

    </div>
</div>